#include <stdio.h>
/**
*main - entry point
*return: always 0 (success)
*Description: the program will print exactly what is in the quote
*/
int main(void)
{
	puts("\"programming is like building a multilingual puzzle");
	return (0);
}
